<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <View
        android:id="@+id/top_banner_view"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:background="@drawable/top_banner_shape"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/icon_app_image_view"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:contentDescription="@string/app_logo_content_description"
        android:src="@drawable/ic_app_book"
        app:layout_constraintBottom_toTopOf="@id/title_text_view"
        app:layout_constraintEnd_toEndOf="@id/top_banner_view"
        app:layout_constraintStart_toStartOf="@id/top_banner_view"
        app:layout_constraintTop_toTopOf="@id/top_banner_view" />

    <TextView
        android:id="@+id/title_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textAlignment="center"
        android:textColor="@color/white"
        android:textSize="25sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@id/top_banner_view"
        app:layout_constraintEnd_toEndOf="@id/top_banner_view"
        app:layout_constraintStart_toStartOf="@id/top_banner_view"
        app:layout_constraintTop_toBottomOf="@id/icon_app_image_view" />
</androidx.constraintlayout.widget.ConstraintLayout>